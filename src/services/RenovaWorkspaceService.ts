export class RenovaWorkspaceService {
  // Placeholder: wire to your renova backend when ready
  async listWorkspaces(): Promise<Array<{id: string; name: string; description?: string;}>> {
    return [];
  }
}
